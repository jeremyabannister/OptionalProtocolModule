import XCTest
@testable import OptionalProtocolModule

final class OptionalProtocolModuleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OptionalProtocolModule().text, "Hello, World!")
    }
}
